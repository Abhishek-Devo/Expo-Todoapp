gpt what is npx create-expo-app@latest --template blnak

files worked with - " data/todos.js, _layout.jsx, index.js tsconfig.js ,"

cmd -> npm run reset-project //clean out the default example project and put it in a directoty we can delete 

git bash -> rm -rf app-example //removes the app-example folder we got from above cmd.

we using javscript so --> 
in tsconfig.json add this line  "**/*.jsx", after  "**/*.tsx". so we can change the files in app directory we have now to jsx extension //chatgpt it more 

search sort() functin working with array 



